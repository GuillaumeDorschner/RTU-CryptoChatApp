const ChatList = () => {
  const chat = (id) => {
    console.log('Chat number ' + id + ' clicked!');
  };

  return (
    <div className="max-w-full p-4" onClick={chat}>
      {/* <h1>{name}</h1>
      <p>{lastMessage}</p> */}
      djksdhfk
    </div>
  );
};

export default ChatList;
