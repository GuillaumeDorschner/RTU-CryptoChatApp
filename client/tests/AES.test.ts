// TODO: make AES the tests

describe('basic', () => {
  it('Is it the same ?', () => {
    const a = Number(11);
    const b = Number(11);
    const sum = a + b;

    expect(sum).toBe(Number(22));
  });
});
